import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.tab.change',
  appName: 'cs-tab-change',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
